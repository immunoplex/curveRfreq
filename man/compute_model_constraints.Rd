% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constraints.R
\name{compute_model_constraints}
\alias{compute_model_constraints}
\title{Compute Parameter Bounds for All Candidate Models}
\usage{
compute_model_constraints(
  data,
  formulas,
  response_variable,
  independent_variable,
  is_log_response,
  fixed_a = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame with response and concentration columns.}

\item{formulas}{Named list of formula objects.}

\item{response_variable}{Character. Response column name.}

\item{independent_variable}{Character. Concentration column name.}

\item{is_log_response}{Logical. Is the response log10-transformed?}

\item{fixed_a}{Numeric or NULL. If non-NULL, \code{a} is fixed and not
bounded. If NULL, adaptive bounds for \code{a} are computed.}

\item{verbose}{Logical.}
}
\value{
Named list of lists, each with \code{lower} and \code{upper} named vectors.
Has attribute \code{"constraint_profile"}.
}
\description{
Inspects the response range to classify the assay scale (high/medium/low),
then builds adaptive bounds for a, b, c, d (and g for 5-param models).
}
\details{
When \code{fixed_a} is NULL, the lower asymptote \code{a} is free and its bounds
are set adaptively from the data. When \code{fixed_a} is non-NULL, \code{a} does
not appear in the formulas (it's baked in), so no bounds are needed.
}
