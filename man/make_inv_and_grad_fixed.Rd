% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_functions.R
\name{make_inv_and_grad_fixed}
\alias{make_inv_and_grad_fixed}
\title{Build Inverse, Gradient, and grad_y Closures for a Model}
\usage{
make_inv_and_grad_fixed(model, y, fixed_a)
}
\arguments{
\item{model}{Character. One of \code{"logistic4"}, \code{"logistic5"}, \code{"loglogistic4"}, \code{"loglogistic5"},
\code{"gompertz4"}.}

\item{y}{Numeric scalar. The observed response value at which to
evaluate the inverse and its derivatives.}

\item{fixed_a}{Numeric scalar or \code{NULL}. If non-\code{NULL}, the lower
asymptote is treated as a fixed external constant.}
}
\value{
A list with three closures:
\describe{
\item{\code{inv(p)}}{Returns the estimated \code{x} (concentration) given
named parameter vector \code{p}.}
\item{\code{grad(p)}}{Returns a named numeric vector of
\eqn{\partial x / \partial \theta_i}.}
\item{\code{grad_y(p)}}{Returns a numeric scalar
\eqn{\partial x / \partial y}.}
}
}
\description{
Constructs a list of three closures — \code{inv}, \code{grad}, and \code{grad_y} —
that evaluate the inverse function, its parameter gradient, and its
response-derivative for a given model at a specific observed response
\code{y}. The closures accept a single named parameter vector \code{p}
(typically from \code{\link[stats:coef]{stats::coef()}}) and are consumed by
\code{\link[=propagate_error_analytic]{propagate_error_analytic()}}.
}
\section{Design}{

Two branches exist depending on whether \code{fixed_a} is supplied:
\describe{
\item{\code{fixed_a} is non-\code{NULL}}{Parameter \code{a} is treated as an
externally known constant. The \code{grad} closure returns partials
for \code{b}, \code{c}, \code{d} (and \code{g} for 5-parameter models) only.
The \verb{_fixed} family of functions is used.}
\item{\code{fixed_a} is \code{NULL}}{Parameter \code{a} is free and must appear
in \code{p}. The full \verb{grad_*()} functions are used, returning
partials for \code{a}, \code{b}, \code{c}, \code{d} (and \code{g}).}
}
}

\examples{
# Free-a example
fns <- make_inv_and_grad_fixed("logistic4", y = 5000, fixed_a = NULL)
p <- c(a = 100, b = 1.5, c = 2, d = 20000)
fns$inv(p)
fns$grad(p)
fns$grad_y(p)

# Fixed-a example
fns2 <- make_inv_and_grad_fixed("logistic4", y = 5000, fixed_a = 100)
p2 <- c(b = 1.5, c = 2, d = 20000)
fns2$inv(p2)
fns2$grad(p2)

}
\seealso{
\code{\link[=propagate_error_analytic]{propagate_error_analytic()}}, \code{\link[=inv_logistic4]{inv_logistic4()}}, \code{\link[=grad_logistic4]{grad_logistic4()}},
\code{\link[=inv_logistic4_fixed]{inv_logistic4_fixed()}}, \code{\link[=grad_inv_logistic4_fixed]{grad_inv_logistic4_fixed()}}
}
