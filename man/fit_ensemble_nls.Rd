% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_one_plate.R
\name{fit_ensemble_nls}
\alias{fit_ensemble_nls}
\title{Fit Ensemble of NLS Models for One Plate}
\usage{
fit_ensemble_nls(
  data,
  formulas,
  model_constraints,
  start_lists,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame of preprocessed standards (already transformed
by \code{\link[curveRcore:preprocess_standards]{curveRcore::preprocess_standards()}}).}

\item{formulas}{Named list of formula objects from \code{\link[curveRcore:build_nls_formulas]{curveRcore::build_nls_formulas()}}.}

\item{model_constraints}{Named list from \code{\link[=compute_model_constraints]{compute_model_constraints()}}.}

\item{start_lists}{Named list from \code{\link[=generate_start_lists]{generate_start_lists()}}.}

\item{verbose}{Logical.}
}
\value{
A named list (one element per model) of lists with:
\describe{
\item{model_name}{Character.}
\item{converged}{Logical.}
\item{fit}{The \code{nlsLM} object, or NULL.}
\item{aic}{Numeric (Inf if failed).}
\item{data}{The data used for fitting.}
}
}
\description{
Fits each model in the ensemble using multi-start Levenberg-Marquardt
NLS, returning the best fit per model (by AIC). Handles convergence
failures gracefully — a model that fails to converge gets
\code{converged = FALSE} in the output.
}
