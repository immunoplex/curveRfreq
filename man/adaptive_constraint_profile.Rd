% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_functions.R
\name{adaptive_constraint_profile}
\alias{adaptive_constraint_profile}
\title{Build an Adaptive Constraint Profile from Observed Data}
\usage{
adaptive_constraint_profile(
  data,
  response_variable,
  is_log_response,
  antigen_settings
)
}
\arguments{
\item{data}{Data frame with at least columns named by
\code{response_variable} and \code{"concentration"}.}

\item{response_variable}{Character. Column name for the response.}

\item{is_log_response}{Logical. \code{TRUE} if data is already
\eqn{\log_{10}}-transformed.}

\item{antigen_settings}{List with constraint metadata, including
\code{l_asy_min_constraint} and \code{l_asy_max_constraint}.}
}
\value{
A named list:
\describe{
\item{y_min, y_max}{Observed response extremes (finite values only).}
\item{dynamic_range}{\code{y_max - y_min}.}
\item{conc_range}{Range of observed concentrations.}
\item{scale_class}{\code{"high"} (MFI-like, e.g. Luminex),
\code{"medium"}, or \code{"low"} (OD/absorbance-like).}
\item{slope_min, slope_max}{Adapted bounds for scale parameter \code{b}.}
\item{g_min, g_max}{Adapted bounds for asymmetry parameter \code{g}.}
\item{conc_pad_frac}{Fraction beyond concentration range for
inflection-point \code{c} bounds.}
\item{d_margin_frac}{Fraction of \code{y_max} for upper-asymptote
\code{d} bounds.}
}
}
\description{
Inspects the response range, dynamic range, and scale to choose
appropriate bounds for nonlinear optimisation. The returned profile
is consumed by every \verb{*_safe_constraint()} function.
}
\details{
Three scale classes are recognised:\tabular{llll}{
   Class \tab Raw threshold \tab Log10 threshold \tab Typical assay \cr
   high \tab \code{y_max > 1000} \tab \code{y_max > 2.5} \tab Luminex MFI \cr
   medium \tab \code{y_max > 10} \tab \code{y_max > 0.5} \tab moderate signals \cr
   low \tab otherwise \tab otherwise \tab ELISA OD/absorbance \cr
}


Narrower dynamic ranges (low scale class) receive wider slope and
asymmetry bounds to avoid near-singular Jacobians during \code{\link[stats:nls]{stats::nls()}}
fitting.
}
\examples{
fake_data <- data.frame(
  mfi = c(100, 500, 5000, 15000, 20000),
  concentration = c(0.01, 0.1, 1, 10, 100)
)
profile <- adaptive_constraint_profile(
  data = fake_data,
  response_variable = "mfi",
  is_log_response = FALSE,
  antigen_settings = list(l_asy_min_constraint = 0,
                          l_asy_max_constraint = 200)
)
str(profile)

}
\seealso{
\code{\link[=logistic5_safe_constraint]{logistic5_safe_constraint()}}, \code{\link[=logistic4_safe_constraint]{logistic4_safe_constraint()}},
\code{\link[=compute_robust_curves]{compute_robust_curves()}}

Other safe-constraints: 
\code{\link{gompertz4_safe_constraint}()},
\code{\link{logistic4_safe_constraint}()},
\code{\link{logistic5_safe_constraint}()},
\code{\link{loglogistic4_safe_constraint}()},
\code{\link{loglogistic5_safe_constraint}()}
}
\concept{safe-constraints}
