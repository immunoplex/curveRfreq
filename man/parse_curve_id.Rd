% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{parse_curve_id}
\alias{parse_curve_id}
\title{Parse Colon-Separated Curve Identifiers into Structured Columns}
\usage{
parse_curve_id(
  data,
  curve_col = "curve_id",
  order,
  schema = NULL,
  keep = NULL,
  sep = ":",
  validate = TRUE
)
}
\arguments{
\item{data}{A data frame containing a column with encoded curve identifiers.}

\item{curve_col}{A character string specifying the name of the column
containing the curve identifiers. Default is \code{"curve_id"}.}

\item{order}{A character vector defining the positional order of fields in
the \code{curve_id}. The length of \code{order} must match the number of elements in
each \code{curve_id} string.}

\item{schema}{Optional named list with elements:
\describe{
\item{required}{Character vector of field names that must be present and non-missing.}
\item{optional}{Character vector of optional field names.}
}
If provided and \code{validate = TRUE}, required fields are checked for
missing or empty values.}

\item{keep}{Optional character vector specifying a subset of parsed fields
to retain in the output. If \code{NULL} (default), all parsed fields are kept.}

\item{sep}{A character string used as the delimiter in \code{curve_id}.
Default is \code{":"}.}

\item{validate}{Logical; if \code{TRUE}, performs validation checks:
\itemize{
\item Ensures each \code{curve_id} has the same number of fields as \code{order}.
\item If \code{schema} is provided, ensures required fields are present and non-missing.
}
Default is \code{TRUE}.}
}
\value{
A data frame containing the original \code{data} columns and additional
parsed columns defined by \code{order} (or a subset if \code{keep} is specified).
}
\description{
Splits a colon-delimited \code{curve_id} column into multiple fields based on a
user-supplied positional \code{order}, with optional validation and the ability
to retain only a subset of parsed fields.
}
\details{
This function assumes that \code{curve_id} values are constructed using a
consistent positional encoding (e.g., via \code{paste(..., sep = ":")}),
where each position corresponds to a known field.

The mapping from \code{curve_id} to columns is purely positional:
the i-th element of the split string is assigned to the i-th name in \code{order}.
Therefore, correct parsing depends on consistent construction of \code{curve_id}
values.

The \code{keep} argument allows users to retain only specific parsed fields,
which is useful for downstream analysis, plotting, or modeling workflows.
}
\examples{
df <- data.frame(
  curve_id = c(
    "proj1:study1:exp1:IgG1:sample:prn:plate1:1000:450",
    "proj2:study2:exp2:IgG2:control:pt:plate2:2000:__none__"
  )
)

order <- c(
  "project_id",
  "study_accession",
  "experiment_accession",
  "feature",
  "source",
  "antigen",
  "plate",
  "nominal_sample_dilution",
  "wavelength"
)

schema <- list(
  required = c("feature", "antigen", "plate", "wavelength"),
  optional = c("project_id")
)

# Parse all fields
parsed_all <- parse_curve_id(
  data = df,
  order = order,
  schema = schema
)

# Keep only selected fields
parsed_subset <- parse_curve_id(
  data = df,
  order = order,
  keep = c("feature", "antigen", "plate")
)

}
