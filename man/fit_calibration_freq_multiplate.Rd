% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_multiplate.R
\name{fit_calibration_freq_multiplate}
\alias{fit_calibration_freq_multiplate}
\title{Fit Frequentist Calibration Curves Across Multiple Curves}
\usage{
fit_calibration_freq_multiplate(
  standards,
  blanks = NULL,
  samples = NULL,
  response_var,
  model_names = c("logistic4", "gompertz4"),
  is_log_response = TRUE,
  is_log_independent = TRUE,
  std_curve_conc,
  fixed_a = NULL,
  cv_x_max = 150,
  n_grid = 200L,
  grid_min_conc = 1e-04,
  grid_max_conc = NULL,
  curve_ids = NULL,
  on_error = c("warn", "stop"),
  verbose = FALSE
)
}
\arguments{
\item{standards}{Data frame. Stacked preprocessed standard curve data.
Must contain a \code{curve_id} column, a response column (named by
\code{response_var}), and a \code{concentration} column — all already on the
fitting scale.}

\item{blanks}{Data frame or NULL. Stacked preprocessed blank data. When
non-NULL, must contain a \code{curve_id} column and a response column (named
by \code{response_var}), both already on the fitting scale. Stored in each
\code{result$blanks} for QA and plotting only — not used in fitting.
NULL (default) leaves \code{result$blanks} empty for every curve.}

\item{samples}{Data frame or NULL. Stacked sample data with a
\code{curve_id} column and the response column (raw measurement scale).}

\item{response_var}{Character. Name of the response column.}

\item{model_names}{Character vector. Models to fit.
Default \code{c("logistic4", "gompertz4")}.}

\item{is_log_response}{Logical. Was the response log10-transformed?
Default TRUE.}

\item{is_log_independent}{Logical. Was concentration log10-transformed?
Default TRUE.}

\item{std_curve_conc}{Numeric. Undiluted standard concentration (raw
scale). Used for grid generation.}

\item{fixed_a}{Numeric or NULL. Fixed lower asymptote on the
\strong{fitting} scale.}

\item{cv_x_max}{Numeric. Default 150.}

\item{n_grid}{Integer. Default 200.}

\item{grid_min_conc}{Numeric. Default 1e-4.}

\item{grid_max_conc}{Numeric or NULL.}

\item{curve_ids}{Optional vector. If supplied, only these curve_ids
are fitted. Default NULL fits all found in \code{standards}.}

\item{on_error}{Character. \code{"warn"} (default) stores NULL and continues;
\code{"stop"} raises on first failure.}

\item{verbose}{Logical.}
}
\value{
A \code{calibration_result_multiplate} object (from curveRcore).
}
\description{
Splits preprocessed \code{standards}, \code{blanks}, and optionally \code{samples} by
\code{curve_id}, calls \code{\link[=fit_calibration_freq]{fit_calibration_freq()}} for each curve, and
collects the results.
}
\details{
\strong{Important:} \code{standards} and \code{blanks} must already be on the fitting
scale. Use \code{\link[curveRcore:preprocess_standards]{curveRcore::preprocess_standards()}} upstream on each curve's
data before stacking, or preprocess the full stacked frame if all
curves share the same settings.
}
