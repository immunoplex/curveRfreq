% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_functions.R
\name{select_model_formulas}
\alias{select_model_formulas}
\title{Select Model Formulas for Standard Curve Fitting}
\usage{
select_model_formulas(
  fixed_constraint,
  response_variable,
  is_log_response,
  model_names
)
}
\arguments{
\item{fixed_constraint}{Numeric or \code{NULL}. If non-\code{NULL}, the lower
asymptote \code{a} is fixed to this value in every formula. Typically
derived from blank-well measurements. Values that are non-positive
or non-finite are rejected with a message, falling back to free-\code{a}
formulas.}

\item{response_variable}{Character. Column name for the assay response
(e.g., \code{"mfi"}, \code{"od"}). Used as the left-hand side of each formula.}

\item{is_log_response}{Logical. If \code{TRUE}, \code{fixed_constraint} is
\eqn{\log_{10}}-transformed before substitution (with a small
epsilon to avoid \eqn{\log(0)}).}

\item{model_names}{a vector of model formulas to be considered.}
}
\value{
Named list of formulas keyed by model name:
\code{"logistic5"}, \code{"loglogistic5"}, \code{"logistic4"}, \code{"loglogistic4"}, \code{"gompertz4"}.
}
\description{
Returns a named list of \code{\link[stats:nls]{stats::nls()}}-compatible formulas for all
candidate models (logistic5, loglogistic5, logistic4, loglogistic4, gompertz4). When \code{fixed_constraint}
is supplied, parameter \code{a} (lower asymptote) is substituted as a
numeric constant in every formula; otherwise \code{a} is a free parameter.
}
\details{
The concentration column is hard-coded as \code{concentration} in all
formulas. The \code{I()} wrappers protect complex sub-expressions from
formula parsing by \code{\link[stats:nls]{stats::nls()}}.
}
\examples{
# Fixed lower asymptote
forms <- select_model_formulas(fixed_constraint = 50,
                               response_variable = "mfi",
                               is_log_response = FALSE,
                               model_names = c("logistic4", "logistic5",
                               "loglogistic4", "loglogistic5",
                               "gompertz4"))
names(forms)
forms$logistic4

# Free lower asymptote
forms_free <- select_model_formulas(fixed_constraint = NULL,
                                    response_variable = "mfi",
                                    is_log_response = FALSE,
                                    model_names = c("logistic4", "logistic5",
                                     "loglogistic4", "loglogistic5",
                                      "gompertz4"))
forms_free$logistic4

}
\seealso{
\code{\link[=logistic4]{logistic4()}}, \code{\link[=logistic5]{logistic5()}}, \code{\link[=loglogistic4]{loglogistic4()}}, \code{\link[=loglogistic5]{loglogistic5()}}, \code{\link[=gompertz4]{gompertz4()}} for the
underlying model functions; \code{\link[=compute_robust_curves]{compute_robust_curves()}} which
consumes these formulas.
}
