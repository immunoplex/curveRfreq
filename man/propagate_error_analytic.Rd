% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_fitting.R
\name{propagate_error_analytic}
\alias{propagate_error_analytic}
\title{Analytic error propagation for inverse sigmoid models}
\usage{
propagate_error_analytic(model, fit, y, se_y = 0, fixed_a, verbose = TRUE)
}
\arguments{
\item{model}{Character string specifying the model form.
One of \code{"Y4"}, \code{"Yd4"}, \code{"Ygomp4"}, \code{"Y5"}, \code{"Yd5"}.}

\item{fit}{A fitted \code{nlsLM} model object (from \code{minpack.lm}).}

\item{y}{Numeric scalar. Observed response value.}

\item{se_y}{Numeric scalar. Standard error of the response \code{y}.
Defaults to \code{0} if unknown.}

\item{fixed_a}{Optional numeric scalar. Fixed lower asymptote.
If supplied, \code{a} is treated as fixed and excluded from uncertainty propagation.}

\item{verbose}{Logical. If \code{TRUE}, prints diagnostic information.}
}
\value{
A list with components:
\describe{
\item{x_est}{Inverse-predicted concentration}
\item{se_x}{Standard error of \code{x_est}}
\item{var_x}{Variance of \code{x_est}}
\item{grad_theta}{Gradient w.r.t. model parameters}
\item{grad_y}{Gradient w.r.t. response \code{y}}
}
}
\description{
Computes the inverse-predicted concentration (\code{x_est}) and its propagated
uncertainty (\code{se_x}) using the delta method for a fitted nonlinear model.
}
\details{
Supports multiple sigmoid model forms (4PL, 5PL, Gompertz variants), and
optionally handles the case where the lower asymptote (\code{a}) is fixed.

Uses the delta method:
\deqn{Var(x) = \nabla_\theta^T \Sigma \nabla_\theta + (\partial x / \partial y)^2 \cdot Var(y)}

If \code{fixed_a} is supplied, gradients are computed using reduced parameter space.
}
