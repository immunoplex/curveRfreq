% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_antigen_plate.R
\name{resolve_curve_settings}
\alias{resolve_curve_settings}
\title{Resolve curve settings for an antigen on a plate}
\usage{
resolve_curve_settings(
  loaded_data,
  antigen_constraints,
  wavelength = WL_NONE,
  verbose = TRUE
)
}
\arguments{
\item{loaded_data}{A list containing plate-level datasets with the following
elements:
\itemize{
\item \code{standards} Standard curve data.
\item \code{blanks} Blank/control data.
\item \code{samples} Sample data.
\item \code{curve_id_lookup} a single row containing the plate-level curve identifier
including antigen, study accession and experiment_accession and the id number.
}}

\item{antigen_constraints}{Data frame or list containing antigen-specific
constraint rules used in \code{obtain_lower_constraint()}.}

\item{wavelength}{Optional wavelength filter. If provided, all datasets are
filtered to the matching normalized wavelength. Defaults to \code{WL_NONE}.}

\item{verbose}{Logical. If \code{TRUE}, prints diagnostic messages including row counts.}
}
\value{
A list containing:
\itemize{
\item \code{plate_standard} Filtered standard curve data.
\item \code{plate_blanks} Filtered blank data.
\item \code{plate_samples} Filtered sample data.
\item \code{antigen_settings} Output of \code{obtain_lower_constraint()}.
\item \code{fixed_a_result} Validated lower asymptote result.
\item \code{std_error_blank} Estimated standard error of blank measurements.
}
}
\description{
Computes modeling inputs and constraints for a selected antigen on a plate,
including lower asymptote handling and blank variability estimates.
}
\details{
This function assumes that plate-level datasets have already been filtered
and derives additional settings needed
for curve fitting.

\strong{Wavelength filtering:}
If \code{wavelength} is provided (not \code{WL_NONE}), all input datasets are filtered
to rows matching the normalized wavelength using \code{normalize_wavelength()}.

\strong{Assumptions:}
\itemize{
\item Input data has already been subset to a single curve/plate context.
\item The \code{standards} dataset must contain at least one row after filtering,
otherwise an error is thrown.
}
}
\examples{
\dontrun{
settings <- resolve_curve_settings(
  loaded_data = plate_data,
  antigen_constraints = constraints
)
}

}
