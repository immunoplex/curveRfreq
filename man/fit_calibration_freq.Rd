% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_calibration.R
\name{fit_calibration_freq}
\alias{fit_calibration_freq}
\title{Fit a Frequentist Calibration Curve (Single Curve)}
\usage{
fit_calibration_freq(
  standards,
  samples = NULL,
  response_var,
  model_names = c("logistic4", "gompertz4"),
  is_log_response = TRUE,
  is_log_independent = TRUE,
  std_curve_conc,
  fixed_a = NULL,
  cv_x_max = 150,
  pcov_threshold = 20,
  min_dynamic_range_log10 = 0.5,
  max_rel_se = 5,
  bound_tol = 1e-04,
  n_grid = 200L,
  grid_min_conc = 1e-04,
  grid_max_conc = NULL,
  curve_id = "1",
  verbose = FALSE
)
}
\arguments{
\item{standards}{Data frame. Preprocessed standard curve data with a
response column (named by \code{response_var}) and a \code{concentration}
column, both already on the fitting scale.}

\item{samples}{Data frame or NULL. Test sample data with the response
column (on the \strong{raw} measurement scale — log-transform is applied
internally for back-calculation). Optionally a \code{dilution} column.}

\item{response_var}{Character. Name of the response column.}

\item{model_names}{Character vector. Models to fit. Must be a subset
of \code{\link[curveRcore:available_models]{curveRcore::available_models()}}.
Default \code{c("logistic4", "gompertz4")}.}

\item{is_log_response}{Logical. Was the response log10-transformed
during preprocessing? Needed to correctly back-transform sample
predictions. Default TRUE.}

\item{is_log_independent}{Logical. Was concentration log10-transformed?
Needed for pcov computation and grid generation. Default TRUE.}

\item{std_curve_conc}{Numeric. Undiluted standard concentration (raw
scale). Used for grid generation.}

\item{fixed_a}{Numeric or NULL. Fixed lower asymptote on the \strong{fitting}
scale (i.e. already log-transformed if \code{is_log_response = TRUE}).
NULL means \code{a} is estimated freely.}

\item{cv_x_max}{Numeric. Cap for percent CV of back-calculated
concentration. Default 150.}

\item{pcov_threshold}{Numeric. Percent CV threshold for LLOQ/ULOQ
determination and the dynamic-range eligibility gate. Default 20.}

\item{min_dynamic_range_log10}{Numeric. Minimum dynamic range (log10
units) required for a model to be eligible for selection. Default 0.5.}

\item{max_rel_se}{Numeric. Maximum relative SE (SE/|estimate|) permitted
for any parameter; models exceeding this are ineligible. Default 5.0.}

\item{bound_tol}{Numeric. Tolerance for the at-bound gate. Default 1e-4.}

\item{n_grid}{Integer. Number of prediction grid points. Default 200.}

\item{grid_min_conc}{Numeric. Minimum grid concentration (raw scale).
Default 1e-4.}

\item{grid_max_conc}{Numeric or NULL. Maximum grid concentration.
NULL uses \code{std_curve_conc}.}

\item{curve_id}{Character or numeric. Identifier for this curve.
Default \code{"1"}.}

\item{verbose}{Logical. Default FALSE.}
}
\value{
A \code{calibration_result} object (from curveRcore).  The
\verb{$selection} component now contains \verb{$assessments} (per-model
eligibility results), \verb{$eligible_models}, and \verb{$fallback}.
}
\description{
Fits an ensemble of nonlinear models to preprocessed standard curve
data for one curve_id, selects the best model by AIC subject to
quantification-eligibility gates, and produces grid and sample
predictions with propagated uncertainty.
}
\details{
\strong{Important:} \code{standards} must already be on the fitting scale.
Both the response column and the \code{concentration} column should be
transformed (e.g. log10) before calling this function. Use
\code{\link[curveRcore:preprocess_standards]{curveRcore::preprocess_standards()}} upstream.
}
