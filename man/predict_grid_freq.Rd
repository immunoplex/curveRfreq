% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_grid.R
\name{predict_grid_freq}
\alias{predict_grid_freq}
\title{Predict Grid with Uncertainty (Frequentist)}
\usage{
predict_grid_freq(
  grid,
  fit,
  model_name,
  fixed_a = NULL,
  se_response = 0,
  cv_x_max = 150,
  pcov_threshold = 20,
  is_log_independent = TRUE,
  is_log_response = TRUE,
  independent_variable = "concentration",
  verbose = FALSE
)
}
\arguments{
\item{grid}{Data frame from \code{\link[curveRcore:generate_prediction_grid]{curveRcore::generate_prediction_grid()}}.
Must contain an \code{x_fit} column (grid points on the fitting scale).}

\item{fit}{The best-fit \code{nlsLM} object.}

\item{model_name}{Character. Model name (must be in
\code{curveRcore::available_models()}).}

\item{fixed_a}{Numeric or NULL. Fixed lower asymptote on fitting scale.
When non-NULL the \code{a} parameter is removed from \code{theta} and \code{Sigma}
before gradient computation.}

\item{se_response}{Numeric. Residual SE of the response on the fitting
scale. Typically \code{summary(fit)$sigma}. Default 0 (parameter uncertainty
only).}

\item{cv_x_max}{Numeric. Hard cap for \code{pcov} and \code{pcov_rmse} (\%). Values
above this are clamped. Default 150.}

\item{pcov_threshold}{Numeric. Precision threshold (\%) used to set
\code{pcov_pass}. A grid point passes when \code{pcov < pcov_threshold}. Default
20. Must be \verb{<= cv_x_max}.}

\item{is_log_independent}{Logical. Is the independent variable (x) on the
log10 scale? When \code{TRUE}, a \code{log10_concentration} column is written as an
alias for \code{x_fit}, and \code{pcov} is computed as
\code{SE_x * log(10) * 100}. Default \code{TRUE}.}

\item{is_log_response}{Logical. Passed to \code{\link[curveRcore:enrich_grid_with_d2y]{curveRcore::enrich_grid_with_d2y()}}.
Default \code{TRUE}.}

\item{independent_variable}{Character. Name of the independent variable
column in \code{grid}. Default \code{"concentration"}.}

\item{verbose}{Logical. Emit per-point diagnostic messages. Default
\code{FALSE}.}
}
\value{
The input \code{grid} data frame with the following columns appended:
\describe{
\item{\code{log10_concentration}}{Alias for \code{x_fit} when
\code{is_log_independent = TRUE}; otherwise \code{NA_real_}. Present
unconditionally so downstream code can always reference it.}
\item{\code{predicted_response}}{Forward model prediction
\eqn{\hat{y} = f(x, \hat{\theta})} on the fitting scale.}
\item{\code{ci_lower}, \code{ci_upper}}{Delta-method 95\\% CI on the response
scale.}
\item{\code{predicted_concentration}}{Back-calculated concentration from
\eqn{\hat{y}}, on the fitting scale.}
\item{\code{se_concentration}}{Delta-method SE of \code{predicted_concentration}.}
\item{\code{pcov}}{Percent CV of back-calculated concentration (\\%), capped
at \code{cv_x_max}.}
\item{\code{pcov_rmse}}{Relative RMSE (\\%) — incorporates both variance and
bias of \code{predicted_concentration} vs \code{x_fit}. Capped at \code{cv_x_max}.}
\item{\code{pcov_pass}}{Logical. \code{TRUE} when \code{pcov < pcov_threshold} and
\code{pcov} is finite.}
\item{\code{d2y_dx2}}{Second derivative
\eqn{d^2(\log_{10} y)/d(\log_{10} x)^2} at each grid point,
added by \code{\link[curveRcore:enrich_grid_with_d2y]{curveRcore::enrich_grid_with_d2y()}}.}
}
}
\description{
For each point on the prediction grid: evaluates the forward model
to get predicted response and delta-method CI (via
\code{\link[curveRcore:compute_curve_ci]{curveRcore::compute_curve_ci()}}), then back-calculates concentration
and propagates uncertainty to get \code{se_concentration} and \code{pcov}.
}
\details{
The \code{se_response} parameter controls observation-level noise in the
delta method. When non-zero, it adds the response variance term
to the concentration uncertainty via the O'Connell et al. (1993)
formula: \verb{Var(x) = grad_theta' * Sigma * grad_theta + (dx/dy)^2 * sigma_y^2}.
}
